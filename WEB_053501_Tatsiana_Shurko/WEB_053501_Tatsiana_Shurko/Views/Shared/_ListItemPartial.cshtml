@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var request = ViewContext.HttpContext.Request;
    var returnUrl = request.Path + request.QueryString.ToUriComponent();
    var domain = request.Host; 
}

 <div class="col-sm-3" style="margin-bottom:15px;">
        <div class="card">
          <div class="card-body" style="text-align:center;">
              <img src="https://@domain/@Model.ImagePath" style="height:100px; width:70px;"/>
            <h5 class="card-title">@Model.Title</h5>
            <p class="card-text">@Model.Discription</p>
            <p class="card-title mt-6">@Model.Price$</p>
                    <p class="mt-2">
                        <a asp-action="Add" asp-controller="Cart" asp-route-id="@Model.Id" asp-route-returnUrl="@returnUrl" class="btn btn-primary">
                            <i class="fa fa-shopping-cart"></i> To Cart
                        </a>
                    </p>
          </div>
        </div>
      </div>